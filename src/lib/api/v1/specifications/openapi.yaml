openapi: 3.1.0
info:
  title: Arcade Locator API
  description: |-
    The Arcade Locator API provides a comprehensive set of endpoints to discover, track, and manage arcade venues and their machine inventory.

    Users can search for arcades by location, find specific games (like Pinball, retro cabinets, or modern rhythm games), and contribute by adding new venues, machines, and related media such as video game movies and shows.

    Whether you're looking for a classic Barcade, a family entertainment center, or a hidden local gem, this API powers the search for your next arcade adventure.
  contact:
    email: bplentl@gmail.com
  license:
    name: GPLv3
    url: https://www.gnu.org/licenses/gpl-3.0.en.html
  version: 0.0.59
externalDocs:
  description: Find out more about the Arcade Locator API
  url: http://swagger.io
servers:
  - url: /identity
    description: Base url to handle authentication requests
  - url: /api/v1
    description: All API endpoints are relative to the base URL.
tags:
  - name: core
    description: Core Operations of the Arcade Locator API
  - name: movies
    description: Everything about Movies
    externalDocs:
      description: Find more movies to add
      url: https://letterboxd.com/pillager/list/video-game-movies/
  - name: shows
    description: Everything about Shows
  - name: extras
    description: Extra data
  - name: authentication
    description: Authentication endpoints for various tasks
  - name: definitions
    description: Shared model and component definitions
  - name: venue
    description: Operations to search and manage Arcade Venues
    externalDocs:
      description: Find out more about our store
      url: http://swagger.io
  - name: user
    description: Operations about user
paths:
  /:
    $ref: "./paths/root.yaml"
  /apikey/{userId}:
    $ref: "./paths/apikey.yaml"
  /games:
    $ref: "./paths/games.yaml"
  /gamesTest:
    $ref: "./paths/gamesTest.yaml"
  /game/{gameId}:
    $ref: "./paths/game.yaml"
  /movies:
    $ref: "./paths/movies.yaml"
  /movie/{movieId}:
    $ref: "./paths/movie.yaml"
  /shows:
    $ref: "./paths/shows.yaml"
  /show/{showId}:
    $ref: "./paths/show.yaml"
  /venue/search:
    $ref: "./paths/venue_search.yaml"
  /venue/{venueId}:
    $ref: "./paths/venue.yaml"
  # /identity/

  /login:
    $ref: "./paths/login.yaml"
  /register:
    $ref: "./paths/register.yaml"
  /token:
    $ref: "./paths/token.yaml"
    # components:
#   securitySchemes:

#     petstore_auth:

#       type: oauth2

#       flows:

#         implicit:

#           authorizationUrl: http://localhost:8080/identity/token

#           scopes:

#             "write:pets": modify pets in your account

#             "read:pets": read your pets

#     api_key:

#       type: apiKey

#       name: api_key

#       in: header

# ApiKeyAuth:

#   $ref: './components/securitySchemes.yaml#/ApiKeyAuth'
